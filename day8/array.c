#include <stdio.h>
int main()
{
    // int arr[] = {1, 2, 3, 4, 5, 6};~
    // int vari;
    // int n = 6; // no of items
    // int temp = n - 1;
    // for (int i = 0; i <= (n - 1) / 2; i++)
    // {
    //     vari = arr[i];
    //     arr[i] = arr[temp];
    //     arr[temp] = vari;
    //     temp--;
    // }
    // printf("%d, %d, %d, %d, %d, %d", arr[0], arr[1], arr[2], arr[3], arr[4], arr[5]);

    float i;
    scanf("%f", &i);
    printf("%f", i);
    return 0;
}